# new-site

Placeholder for an undecided web project. Clean, separate from the `Bundle`
(pf-snipe) tool. Static site — fast, free to host, easy to grow.

## Structure

```
public/
  index.html          # the page
  assets/styles.css   # styling
```

## Run locally

Open `public/index.html` in a browser, or serve it:

```
npx serve public
```

## Deploy

Static — hosts free on Cloudflare Pages, Netlify, or GitHub Pages.
Wire-up TBD once the project goal is set.

## Status

Scaffold only. Project goal not yet decided — fill in the title, tagline,
and main action in `public/index.html` when ready.
